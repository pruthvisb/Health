{
  "name": "healthsphere-root",
  "version": "1.0.0",
  "description": "Root script wrapper for HealthSphere AI monorepo",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "backend:dev": "npm run start:dev --prefix backend",
    "backend:build": "npm run build --prefix backend",
    "backend:prisma": "npx prisma generate --schema=backend/prisma/schema.prisma"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
